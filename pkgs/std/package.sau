; Name of the package
[var package_name "std"]

; Indicate that we need to include some sub-modules
[var submodules [list "io" "random"]]

; No shared libs at this level

; Top level files for the std package
[var source_files [list
   "version.sau"
]]